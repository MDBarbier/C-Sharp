To run just the tests in a specific tag, run the tests via the command line like this:

.\nunit3-console.exe "C:\Code\local_projects\specflowtags\tags\bin\Debug\tags.dll" --where="cat == mytag2"


This works across feature files too.